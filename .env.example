# Copy to .env and set your real values. Never commit .env.

# Password for the Supabase edge function (gemini-proxy).
# Must match the value configured in the function. Sent in X-App-Secret header.
EXPO_PUBLIC_APP_SECRET=
